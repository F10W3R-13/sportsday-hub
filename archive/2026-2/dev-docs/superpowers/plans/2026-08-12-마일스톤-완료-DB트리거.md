# 마일스톤 완료 DB 트리거 이관(Phase 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 마일스톤 자동완료 판정을 클라이언트(`milestone-sync.ts`)에서 DB `AFTER ROW` 트리거로 이관하고, 클라이언트 동기화 코드를 제거한다. 동작 변경 없음.

**Architecture:** `checklist_items`의 AFTER INSERT/UPDATE/DELETE 트리거가 같은 트랜잭션에서 영향받은 마일스톤의 `completed`를 재계산(`recompute_milestone` 헬퍼). 1회성 백필로 기저 정합. 클라이언트는 `router.refresh()`만 남긴다.

**Tech Stack:** PostgreSQL/Supabase (plpgsql 트리거), Next.js, TanStack Query, Vitest.

**Spec:** `docs/superpowers/specs/2026-08-12-마일스톤-완료-DB트리거-design.md`

## Global Constraints

- 마이그레이션 파일명: `0012_milestone_completion_trigger.sql`, `BEGIN; ... COMMIT;` 래핑(기존 규칙).
- 트리거 함수는 `0003_audit_softdelete.sql`의 `audit_capture` 패턴(plpgsql, `create trigger ... after ... for each row`)을 따른다.
- `recompute_milestone`만 `security definer set search_path = public, pg_temp`. 트리거 함수 자체는 plain `language plpgsql`.
- 동작 변경 금지 — 기존 클라이언트 의미론(순수 마일스톤 미건드림, 롤백 규칙)을 DB가 동일 재현.
- 판정 규칙은 TS(`milestone-completion.ts`)와 SQL(`recompute_milestone`)에 이원화 — 규칙 변경 시 양쪽 갱신.
- 한글 경로에서 `pnpm`이 죽으므로 모든 스크립트는 `npm`/`npx` 사용.

## Pre-execution (중요)

- **Task 4(클라이언트 정리)는 PR #4 머지 후 실행**: 이 브랜치는 main 기반이라 `milestone-sync.ts`가 PR #4 이전 상태. PR #4가 `milestone-sync.ts`를 수정(로깅 추가)하므로, Task 4에서 파일 삭제 시 modify/delete 충돌 회피를 위해 **PR #4 머지 후 이 브랜치를 main에 rebase**한 뒤 Task 4를 실행한다.
- **Task 1~3(마이그레이션/검증)은 PR #4와 무관** — 언제든 실행 가능.
- 작업 브랜치: `feat/마일스톤-완료-DB트리거` (이미 생성됨, spec 커밋 포함).

## File Structure

| 파일 | 역할 | 작업 |
|------|------|------|
| `sportsday-hub/scripts/verify-milestone-trigger.sql` | 트리거 동작 검증(ROLLBACK 기반, 파괴 없음) | Create |
| `sportsday-hub/supabase/migrations/0012_milestone_completion_trigger.sql` | 헬퍼 함수 + 트리거 함수 + 부착 + 백필 | Create |
| `sportsday-hub/lib/mutations/checklist.ts` | `syncMilestoneCompletion` import·3호출 제거 → `router.refresh()` | Modify |
| `sportsday-hub/lib/milestone-sync.ts` | 사이드이펙트 작성기 (트리거가 대체) | Delete |
| `sportsday-hub/lib/milestone-completion.ts` | 순수 판정 함수 (유지 — Phase 2용) | 변경 없음 |

---

### Task 1: 검증 SQL 스크립트 (기대동작 명세 — TDD의 test 역할)

**Files:**
- Create: `sportsday-hub/scripts/verify-milestone-trigger.sql`

**Interfaces:**
- Produces: dev Supabase에서 실행 가능한, 5개 시나리오를 ROLLBACK 안에서 검증하는 스크립트. Task 3에서 실행한다.

**참고:** DB 통합 테스트 인프라가 없으므로, 이 스크립트가 트리거의 "test" 역할. Task 2 마이그레이션 적용 전엔 실행할 수 없다(트리거가 없으므로). Task 2→3 순서로 실행.

- [ ] **Step 1: 스크립트 파일 생성**

`sportsday-hub/scripts/verify-milestone-trigger.sql`:

```sql
-- 마일스톤 완료 동기화 트리거 검증
-- spec: docs/superpowers/specs/2026-08-12-마일스톤-완료-DB트리거-design.md §8
--
-- 파괴 없음: 전체가 BEGIN ... ROLLBACK 안에서 동작. dev Supabase에서만 실행할 것.
-- 실행:  psql "$DATABASE_URL" -f sportsday-hub/scripts/verify-milestone-trigger.sql
--        또는 Supabase 대시보드 SQL 에디터에 붙여넣기.
-- 판정:  "PASS" NOTICE 5개가 모두 기대값과 일치하면 합격.

begin;

-- 직전 실행 잔류 방지(롤백 누락 시)
delete from public.checklist_items
  where id in ('22222222-2222-2222-2222-222222222221','22222222-2222-2222-2222-222222222222','22222222-2222-2222-2222-222222222223');
delete from public.milestones
  where id in ('11111111-1111-1111-1111-111111111111','33333333-3333-3333-3333-333333333333');

-- fixture: 자식 2개인 테스트 마일스톤(team_id는 nullable → null)
insert into public.milestones (id, date, title, completed)
values ('11111111-1111-1111-1111-111111111111', '2026-12-31', '[VERIFY] 트리거', false);

insert into public.checklist_items (id, milestone_id, content, completed, sort_order)
values
  ('22222222-2222-2222-2222-222222222221', '11111111-1111-1111-1111-111111111111', '[VERIFY] 자식1', false, 1),
  ('22222222-2222-2222-2222-222222222222', '11111111-1111-1111-1111-111111111111', '[VERIFY] 자식2', false, 2);

-- ===== S1: 자식 전부 완료 → completed=true =====
update public.checklist_items set completed = true
  where id in ('22222222-2222-2222-2222-222222222221','22222222-2222-2222-2222-222222222222');
do $$ declare c boolean; begin
  select completed into c from public.milestones where id='11111111-1111-1111-1111-111111111111';
  if c is true then raise notice 'S1 PASS 전부완료 → true (기대=true)';
  else raise notice 'S1 FAIL 기대=true 실제=%', c; end if;
end $$;

-- ===== S2: 하나 해제 → 롤백 completed=false =====
update public.checklist_items set completed = false
  where id='22222222-2222-2222-2222-222222222221';
do $$ declare c boolean; begin
  select completed into c from public.milestones where id='11111111-1111-1111-1111-111111111111';
  if c is false then raise notice 'S2 PASS 하나해제 → false (기대=false)';
  else raise notice 'S2 FAIL 기대=false 실제=%', c; end if;
end $$;

-- ===== S3: 완료된 마일스톤 밑에 미완료 항목 추가 → 롤백 =====
update public.checklist_items set completed = true
  where id='22222222-2222-2222-2222-222222222221';  -- 다시 전부 완료 → milestone true
insert into public.checklist_items (id, milestone_id, content, completed, sort_order)
values ('22222222-2222-2222-2222-222222222223','11111111-1111-1111-1111-111111111111','[VERIFY] 자식3(미완료)', false, 3);
do $$ declare c boolean; begin
  select completed into c from public.milestones where id='11111111-1111-1111-1111-111111111111';
  if c is false then raise notice 'S3 PASS 미완료추가 → 롤백 false (기대=false)';
  else raise notice 'S3 FAIL 기대=false 실제=%', c; end if;
end $$;

-- ===== S4: 마지막 미완료 항목 soft-delete → 자동 완료 =====
update public.checklist_items set deleted_at = now()
  where id='22222222-2222-2222-2222-222222222223';
do $$ declare c boolean; begin
  select completed into c from public.milestones where id='11111111-1111-1111-1111-111111111111';
  if c is true then raise notice 'S4 PASS 마지막미완료 soft-delete → true (기대=true)';
  else raise notice 'S4 FAIL 기대=true 실제=%', c; end if;
end $$;

-- ===== S5: 순수 마일스톤(자식 없음)은 recompute가 건드리지 않음 =====
insert into public.milestones (id, date, title, completed)
values ('33333333-3333-3333-3333-333333333333', '2026-12-31', '[VERIFY] 순수', false);
perform public.recompute_milestone('33333333-3333-3333-3333-333333333333');  -- total=0 → noop
do $$ declare c boolean; begin
  select completed into c from public.milestones where id='33333333-3333-3333-3333-333333333333';
  if c is false then raise notice 'S5 PASS 순수마일스톤 noop (기대=false 유지)';
  else raise notice 'S5 FAIL 기대=false 실제=%', c; end if;
end $$;

-- (참고) milestone_id 이동(UPDATE로 마일스톤 변경)은 앱에 해당 mutation이 없으므로
-- 런타임 검증에서 제외. 트리거 함수의 OLD+NEW 처리는 코드 리뷰로 확인(spec §4-2).

rollback;
```

- [ ] **Step 2: 커밋**

```bash
cd "C:/Users/0616y/Downloads/26-2 스포츠데이기획"
git add sportsday-hub/scripts/verify-milestone-trigger.sql
git commit -m "test(db): 마일스톤 완료 트리거 검증 SQL 스크립트 (5시나리오, ROLLBACK)"
```

---

### Task 2: 마이그레이션 0012 — 헬퍼 + 트리거 함수 + 부착 + 백필

**Files:**
- Create: `sportsday-hub/supabase/migrations/0012_milestone_completion_trigger.sql`

**Interfaces:**
- Produces: `public.recompute_milestone(p_id uuid)` (핵심 재계산, security definer), `public.sync_milestone_completion()` (트리거 함수), 트리거 `trg_sync_milestone_completion`. Task 3에서 dev DB에 적용 후 Task 1 스크립트로 검증.

- [ ] **Step 1: 마이그레이션 파일 생성**

`sportsday-hub/supabase/migrations/0012_milestone_completion_trigger.sql`:

```sql
-- 0012: 마일스톤 completed 자동동기화를 DB 트리거로 이관
-- spec: docs/superpowers/specs/2026-08-12-마일스톤-완료-DB트리거-design.md
-- 기존 클라이언트(lib/milestone-sync.ts)를 대체 — 모든 checklist_items 쓰기 경로를
-- 트랜잭션 단위로 커버. 동작은 기존과 동일(책임만 이관).

BEGIN;

-- ===== (1) 핵심 재계산 로직 — 1개 마일스톤 completed를 현재 자식 상태 기준으로 갱신 =====
-- 트리거·백필·디버그가 공유. security definer로 호출자 RLS와 무관하게 DB 불변조건 유지.
create or replace function public.recompute_milestone(p_id uuid)
returns void as $$
declare
  total int;
  done int;
  cur_completed boolean;
begin
  -- 자식 체크리스트(soft-delete 제외) 집계
  select count(*), count(*) filter (where completed)
    into total, done
    from public.checklist_items
    where milestone_id = p_id
      and deleted_at is null;

  -- 현재 마일스톤 completed
  select completed into cur_completed
    from public.milestones
    where id = p_id;
  if not found then
    return;  -- 마일스톤이 없으면(삭제됨) noop
  end if;

  -- 순수 마일스톤(자식 없음) = 사용자 직접 관리 → 건드리지 않음
  if total = 0 then
    return;
  end if;

  -- 멱등: 실제로 바뀔 때만 UPDATE
  if done = total then
    if coalesce(cur_completed, false) is false then
      update public.milestones set completed = true where id = p_id;
    end if;
  else
    if coalesce(cur_completed, false) is true then
      update public.milestones set completed = false where id = p_id;
    end if;
  end if;
end;
$$ language plpgsql
  security definer
  set search_path = public, pg_temp;

-- ===== (2) 트리거 함수 — NEW/OLD에서 영향받은 마일스톤 추출해 recompute 호출(중복제거) =====
create or replace function public.sync_milestone_completion()
returns trigger as $$
begin
  -- UPDATE/DELETE: OLD.milestone_id
  if tg_op in ('UPDATE','DELETE') and old.milestone_id is not null then
    perform public.recompute_milestone(old.milestone_id);
  end if;

  -- INSERT/UPDATE: NEW.milestone_id (UPDATE이고 OLD와 같으면 위에서 처리했으므로 skip)
  if tg_op in ('INSERT','UPDATE') and new.milestone_id is not null then
    if not (tg_op = 'UPDATE' and old.milestone_id is not distinct from new.milestone_id) then
      perform public.recompute_milestone(new.milestone_id);
    end if;
  end if;

  return coalesce(new, old);
end;
$$ language plpgsql;

-- ===== (3) 트리거 부착 (재실행 안전) =====
drop trigger if exists trg_sync_milestone_completion on public.checklist_items;
create trigger trg_sync_milestone_completion
  after insert or update or delete on public.checklist_items
  for each row execute function public.sync_milestone_completion();

-- ===== (4) 1회성 백필 — 현재 checklist 상태 기준으로 모든 마일스톤 재동기화 =====
-- 과거 클라이언트 sync가 놓친 부정합(trash-view류) 보정 → 트리거가 깨끗한 기저에서 시작.
-- milestones 직접 UPDATE이므로 checklist 트리거는 발화하지 않음.
select public.recompute_milestone(id) from public.milestones where deleted_at is null;

COMMIT;
```

- [ ] **Step 2: SQL 구문 정적 검토 (적용 전)**

마이그레이션을 눈으로 다시 검토:
- `recompute_milestone`: `if not found` 직후의 `found`는 바로 앞 `select ... into` 반영 (PostgreSQL 규칙). ✅
- `count(*) filter (where completed)` — 표준 집계 구문. ✅
- `is not distinct from` — null-safe 비교(milestone_id 이동 감지). ✅
- 트리거 함수가 `coalesce(new, old)` 반환 — DELETE에선 new가 null이므로 old 반환. ✅

- [ ] **Step 3: 커밋**

```bash
git add sportsday-hub/supabase/migrations/0012_milestone_completion_trigger.sql
git commit -m "feat(db): 0012 마일스톤 완료 자동동기화 트리거 + 백필"
```

---

### Task 3: dev DB 적용 + 검증 스크립트 실행 (수동 게이트)

**Files:** (변경 없음 — 검증만)

**Interfaces:**
- Consumes: Task 1 스크립트, Task 2 마이그레이션.
- Produces: 5개 시나리오 PASS 확인. 이 게이트를 통과해야 Task 4로 진행.

**참고:** DB 통합 테스트 인프라가 없어 사람이 실행·확인한다 (회고 "수동검증" 교훈 — 이 부분만 인간 게이트).

- [ ] **Step 1: dev Supabase에 0012 적용**

Supabase 대시보드 SQL 에디터 또는 `psql`로 `0012_milestone_completion_trigger.sql` 전체를 실행.
예상: 쿼리 성공, `recompute_milestone` 백필로 일부 마일스톤 `completed`가 보정될 수 있음(로그로 확인).

- [ ] **Step 2: 검증 스크립트 실행**

`verify-milestone-trigger.sql` 전체를 동일 DB에서 실행.
예상 출력(NOTICE 5개):
```
S1 PASS 전부완료 → true (기대=true)
S2 PASS 하나해제 → false (기대=false)
S3 PASS 미완료추가 → 롤백 false (기대=false)
S4 PASS 마지막미완료 soft-delete → true (기대=true)
S5 PASS 순수마일스톤 noop (기대=false 유지)
```
`ROLLBACK`로 마무리되므로 fixture는 남지 않는다.

- [ ] **Step 3: 판정**

5개가 모두 `PASS`면 게이트 통과 → Task 4 진행.
하나라도 `FAIL`이면:
- `recompute_milestone` 로직(total/done/cur 분기)을 다시 점검.
- 기존 클라이언트 `milestone-completion.ts`의 `shouldCompleteMilestone`/`findMilestonesToReopen`과 분기를 대조(spec §10 이원화 노트).
- 수정 후 마이그레이션 재적용(드롭 후 재생성) → 재실행.

(이 태스크는 코드 커밋을 만들지 않는다 — 검증 게이트만.)

---

### Task 4: 클라이언트 정리 — sync 호출 제거 + milestone-sync.ts 삭제

> **PR #4 머지 + 이 브랜치 main rebase 후에 실행** (Pre-execution 참고).

**Files:**
- Modify: `sportsday-hub/lib/mutations/checklist.ts:9`(import 제거), `:33,71,96`(3호출 → `router.refresh()`)
- Delete: `sportsday-hub/lib/milestone-sync.ts`

**Interfaces:**
- Produces: `syncMilestoneCompletion`에 대한 모든 참조 제거. 트리거가 동기화를 담당하므로 `router.refresh()`만으로 갱신된 마일스톤을 클라이언트가 pull(같은 트랜잭션이라 레이스 없음).

- [ ] **Step 1: import 제거**

`sportsday-hub/lib/mutations/checklist.ts`에서 이 줄 삭제:
```ts
import { syncMilestoneCompletion } from '@/lib/milestone-sync'
```

- [ ] **Step 2: 3곳 호출을 router.refresh()로 교체**

3곳 모두 동일 패턴. 각각:
```ts
      void syncMilestoneCompletion().then(() => router.refresh())
```
를
```ts
      void router.refresh()
```
로 변경. (체크 토글·항목 추가·항목 삭제의 `onSuccess`)

- [ ] **Step 3: milestone-sync.ts 삭제**

```bash
git rm sportsday-hub/lib/milestone-sync.ts
```

- [ ] **Step 4: 타입 게이트 + 단위 테스트 검증**

```bash
cd "C:/Users/0616y/Downloads/26-2 스포츠데이기획/sportsday-hub"
npx tsc --noEmit
```
예상: exit 0 (`syncMilestoneCompletion` 참조가 전부 제거됐으므로).

```bash
npm test
```
예상: 44/44 통과 — `milestone-completion.test.ts`(순수 함수 13개)는 유지되므로 그대로 green.

- [ ] **Step 5: 수동 UI 확인 (트리거 적용된 dev 환경에서)**

- 마일스톡 하위 체크리스트를 끝까지 체크 → 마일스톤이 완료로 표시되는지.
- 하나 해제 → 마일스톤이 미완료로 롤백되는지.
- (`router.refresh()` 후 서버 데이터가 당겨지므로 1~2초 내 반영 — Phase 2 optimistic UI가 즉시 반영을 담당.)

- [ ] **Step 6: 커밋**

```bash
git add sportsday-hub/lib/mutations/checklist.ts
git commit -m "refactor: 클라이언트 마일스톤 동기화 제거 — DB 트리거(0012)로 이관"
```
(`milestone-sync.ts`는 Step 3의 `git rm`에 포함.)

---

## 배포 순서 (구현 완료 후 — PR 설명에 명시)

```
1) 0012 마이그레이션을 production DB에 적용 (트리거 활성 + 백필)
   → 구클라이언트 sync도 살아있음 → 양쪽 실행(멱등) ✅ 안전
2) 본 PR 머지 → Vercel이 클라이언트 제거 코드를 production에 배포
```
- 역순 금지: 본 PR(클라이언트 제거)을 먼저 머지하면 트리거 적용 전까지 동기화가 끊김.
- 롤백: 트리거에 문제 시 `drop trigger trg_sync_milestone_completion` → 클라이언트 코드를 직전 커밋으로 되돌리면 구 sync가 복원(단, 본 PR 머지 이전 상태에서만 양쪽 공존하므로 머지 전 검증을 철저히).

## Self-Review (작성자 점검)

**1. Spec coverage** — spec의 각 절이 어디에 구현되는가:
- §3 아키텍처 → Task 2(트리거) + Task 4(클라이언트 제거) ✅
- §4 트리거 로직 → Task 2 Step 1의 함수 2개 ✅
- §5 엣지케이스 → Task 1 검증 5시나리오(S1~S5) + milestone_id 이동은 코드 리뷰(spec §4-2) ✅
- §6 마이그레이션/백필 → Task 2 (1)(2)(3)(4) ✅
- §7 배포 순서 → "배포 순서" 섹션 ✅
- §8 검증 → Task 1 + Task 3 ✅
- §9 클라이언트 정리 → Task 4 ✅
- §10 이원화 노트 → Global Constraints에 명시 ✅
- §11 위험 → "배포 순서" 롤백 + Task 3 FAIL 대응 ✅

**2. Placeholder scan** — TBD/TODO/"적절히 처리" 없음. 모든 SQL/명령 완전 기술. ✅

**3. Type/signature 일치** — `recompute_milestone(p_id uuid)`, `sync_milestone_completion()` 이름이 Task 2(정의)·Task 1(검증 S5 호출)·Task 4(제거 대상은 `syncMilestoneCompletion` TS함수, 별개)에서 일관. ✅

**4. 누락** — `milestone_id` 이동(UPDATE)이 런타임 검증에서 빠졌으나 앱에 해당 mutation이 없고 트리거 함수 코드로 처리됨(spec §4-2, Task 1 주석으로 명시). 허용 범위.
