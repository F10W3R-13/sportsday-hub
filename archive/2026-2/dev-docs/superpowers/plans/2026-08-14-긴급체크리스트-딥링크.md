# 긴급 체크리스트 딥링크 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대시보드 '긴급 체크리스트' 위젯 항목 클릭 시 해당 팀 워크스페이스의 체크리스트 탭이 열리며, 해당 항목으로 부드럽게 스크롤 후 2.5초 하이라이트했다 소멸.

**Architecture:** URL 쿼리(`?tab=&focus=`)가 단일 진실 원천. 위젯은 `<Link>`로 URL을 생성하고, `TeamTabs`는 URL에서 탭을 직접 파생(로컬 state 없음), `ChecklistPanel`이 `focusItemId`를 받아 스크롤+CSS 원샷 하이라이트 수행. 스펙: `docs/superpowers/specs/2026-08-14-긴급체크리스트-딥링크-design.md`

**Tech Stack:** Next.js 16 App Router, React 19, Radix Tabs(shadcn), Tailwind v4(oklch 테마 변수), vitest

## Global Constraints

- 타입체크 `strict` 통과, ESLint 에러 0건, 기존+신규 테스트 전부 통과 — 모든 태스크 종료 시점에 성립해야 함
- UI 문구는 한국어, 기존 코드 스타일(싱글 쿼트·세미콜론, 컴포넌트 내 주석은 제약 설명만) 따름
- DB·API 라우트·쿼리 레이어(`lib/queries/*`)·뮤테이션 일절 변경 금지
- 명령어는 전부 `sportsday-hub/` 디렉터리에서 실행
- 커밋 메시지: 프로젝트 관행(conventional commits + 한국어 설명)

---

### Task 1: URL 빌더 순수함수 (TDD)

**Files:**
- Create: `sportsday-hub/lib/checklist-focus-url.ts`
- Test: `sportsday-hub/tests/checklist-focus-url.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `buildChecklistFocusUrl(teamId: string | null | undefined, itemId: string): string | null` — Task 3 위젯이 사용

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// tests/checklist-focus-url.test.ts
import { describe, it, expect } from 'vitest'
import { buildChecklistFocusUrl } from '@/lib/checklist-focus-url'

describe('lib/checklist-focus-url — 위젯 딥링크 URL 생성', () => {
  it('팀 ID + 항목 ID로 체크리스트 탭 포커스 URL 조합', () => {
    expect(buildChecklistFocusUrl('budget', 'item-42')).toBe(
      '/team/budget?tab=checklist&focus=item-42'
    )
  })

  it('teamId 없음 → null (호출측에서 링크 렌더 안 함)', () => {
    expect(buildChecklistFocusUrl(null, 'item-1')).toBeNull()
    expect(buildChecklistFocusUrl(undefined, 'item-1')).toBeNull()
    expect(buildChecklistFocusUrl('', 'item-1')).toBeNull()
  })
})
```

- [ ] **Step 2: 테스트 실행 — 모듈 없음으로 실패 확인**

Run: `npx vitest run tests/checklist-focus-url.test.ts`
Expected: FAIL — `Failed to resolve import "@/lib/checklist-focus-url"`

- [ ] **Step 3: 최소 구현**

```ts
// lib/checklist-focus-url.ts
// 긴급 체크리스트 위젯 → 팀 체크리스트 탭 특정 항목 딥링크 URL.
// teamId가 없으면 링크를 만들 수 없어 null (호출측은 비클릭 행으로 렌더).
export function buildChecklistFocusUrl(
  teamId: string | null | undefined,
  itemId: string
): string | null {
  if (!teamId) return null
  return `/team/${teamId}?tab=checklist&focus=${itemId}`
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/checklist-focus-url.test.ts`
Expected: PASS — `Tests 2 passed`

- [ ] **Step 5: 커밋**

```bash
git add sportsday-hub/lib/checklist-focus-url.ts sportsday-hub/tests/checklist-focus-url.test.ts
git commit -m "feat: 긴급 체크리스트 딥링크 URL 빌더 순수함수"
```

---

### Task 2: 하이라이트 키프레임 CSS

**Files:**
- Modify: `sportsday-hub/app/globals.css` — 파일 끝(`@layer base` 블록 뒤 어디든)에 추가

**Interfaces:**
- Consumes: 테마 변수 `var(--primary)` (oklch)
- Produces: CSS 클래스 `.checklist-focus-flash` — Task 4 ChecklistPanel이 사용

- [ ] **Step 1: 키프레임 + 클래스 추가**

`app/globals.css` 파일 맨 끝에 추가:

```css
/* 긴급 체크리스트 위젯 딥링크 도착 지점 하이라이트 — 2.5초 원샷 후 자연 소멸.
   forwards로 끝나면 완전 투명 → JS 타이머·클래스 제거 코드 불필요. */
@keyframes checklistFocusFlash {
  0% {
    box-shadow: 0 0 0 3px color-mix(in oklab, var(--primary) 55%, transparent);
  }
  60% {
    box-shadow: 0 0 0 3px color-mix(in oklab, var(--primary) 30%, transparent);
  }
  100% {
    box-shadow: 0 0 0 3px transparent;
  }
}

.checklist-focus-flash {
  animation: checklistFocusFlash 2.5s ease-out forwards;
}
```

- [ ] **Step 2: 빌드 게이트 확인 (CSS 구문 오류 방지)**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | tail -3`
Expected: lint 에러 0건, tsc clean, 테스트 전부 통과

- [ ] **Step 3: 커밋**

```bash
git add sportsday-hub/app/globals.css
git commit -m "feat: 딥링크 도착 하이라이트 원샷 애니메이션 (checklistFocusFlash)"
```

---

### Task 3: 위젯 항목 링크화

**Files:**
- Modify: `sportsday-hub/components/dashboard/urgent-checklist.tsx` (import부 + `urgent.map` 렌더부)

**Interfaces:**
- Consumes: `buildChecklistFocusUrl` (Task 1)
- Produces: 없음 (이 단계의 링크는 `?tab=&focus=`로 이동만 하고, 탭·스크롤 반응은 Task 4·5가 담당 — 중간 상태로 안전)

- [ ] **Step 1: import 추가**

파일 상단 import 마지막에 추가:

```ts
import Link from 'next/link'
import { buildChecklistFocusUrl } from '@/lib/checklist-focus-url'
```

- [ ] **Step 2: 항목 렌더부 교체**

`{urgent.map(({ item, tier, daysFromToday }) => { ... })}` 내부 전체를 아래로 교체 (기존 마크업을 `rowContent`로 추출해 Link/Div 양쪽 재사용):

```tsx
{urgent.map(({ item, tier, daysFromToday }) => {
  const team = item.team_id ? teamMap.get(item.team_id) : null
  const href = buildChecklistFocusUrl(item.team_id, item.id)
  const rowContent = (
    <>
      <span className={`w-16 shrink-0 text-xs font-medium ${labelColor(tier)}`}>
        {dateLabel(tier, daysFromToday)}
      </span>
      <span className="min-w-0 flex-1 truncate">{item.content}</span>
      {team && (
        <span
          className="shrink-0 rounded px-1.5 py-0.5 text-xs"
          style={{
            backgroundColor: `${team.color}20`,
            color: team.color,
          }}
        >
          {team.name}
        </span>
      )}
    </>
  )
  return href ? (
    <Link
      key={item.id}
      href={href}
      className={`flex items-center gap-3 rounded-md border p-3 text-sm transition-colors hover:bg-accent ${tierStyle(tier)}`}
    >
      {rowContent}
    </Link>
  ) : (
    <div
      key={item.id}
      className={`flex items-center gap-3 rounded-md border p-3 text-sm ${tierStyle(tier)}`}
    >
      {rowContent}
    </div>
  )
})}
```

- [ ] **Step 3: 게이트 확인**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | tail -3`
Expected: 전부 통과 (기존 76 + 신규 2 = 78)

- [ ] **Step 4: 커밋**

```bash
git add sportsday-hub/components/dashboard/urgent-checklist.tsx
git commit -m "feat: 긴급 체크리스트 위젯 항목 링크화 — 팀 체크리스트 딥링크"
```

---

### Task 4: ChecklistPanel 포커스 스크롤·하이라이트

**Files:**
- Modify: `sportsday-hub/components/team/checklist-panel.tsx` (import부, props, 신규 useEffect, 항목 행)

**Interfaces:**
- Consumes: CSS 클래스 `.checklist-focus-flash` (Task 2)
- Produces: 선택적 prop `focusItemId?: string | null` — Task 5 TeamTabs가 전달. prop은 선택적이라 이 단계에서 기존 호출측(TeamTabs)도 그대로 컴파일됨

- [ ] **Step 1: useEffect import 추가**

```ts
import { useEffect, useState } from 'react'
```

(기존 `import { useState } from 'react'` 교체)

- [ ] **Step 2: props에 focusItemId 추가**

컴포넌트 시그니처를 아래로 교체:

```tsx
export function ChecklistPanel({
  items,
  milestones,
  teamId,
  focusItemId = null,
}: {
  items: ChecklistItem[]
  milestones: Milestone[]
  teamId: TeamId | null
  // 대시보드 긴급 위젯 딥링크(?focus=) 도착 시 스크롤+하이라이트 대상 항목 ID
  focusItemId?: string | null
}) {
```

- [ ] **Step 3: 스크롤 effect 추가**

기존 state 선언(`prevItems` 블록) 뒤에 추가:

```tsx
  // 딥링크 도착: 대상 항목을 화면 중앙으로 부드럽게. 요소가 없으면(삭제된
  // 항목 등) 조용히 무시. setState 없음 — set-state-in-effect 회피.
  useEffect(() => {
    if (!focusItemId) return
    document
      .getElementById(`checklist-item-${focusItemId}`)
      ?.scrollIntoView({ behavior: 'smooth', block: 'center' })
  }, [focusItemId])
```

- [ ] **Step 4: 항목 행에 앵커 + 조건부 클래스**

`groupItems.map` 안의 항목 `<div>` 여는 태그를 교체:

```tsx
<div
  key={item.id}
  id={`checklist-item-${item.id}`}
  className={`flex items-start gap-3 rounded-md border p-2 ${
    item.id === focusItemId ? 'checklist-focus-flash' : ''
  }`}
>
```

- [ ] **Step 5: 게이트 확인**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | tail -3`
Expected: 전부 통과

- [ ] **Step 6: 커밋**

```bash
git add sportsday-hub/components/team/checklist-panel.tsx
git commit -m "feat: ChecklistPanel 딥링크 포커스 — 앵커·중앙 스크롤·원샷 하이라이트"
```

---

### Task 5: TeamTabs URL 연동 (기능 완성)

**Files:**
- Modify: `sportsday-hub/components/team/team-tabs.tsx` — 파일 전면 재작성 (구조: Suspense 래퍼 + Inner)

**Interfaces:**
- Consumes: `focusItemId` prop (Task 4), Radix `Tabs` controlled API
- Produces: 없음 — 이 태스크로 위젯→탭→스크롤까지 엔드투엔드 동작 완성

- [ ] **Step 1: 파일 전체를 아래로 교체**

기존 props/마크업은 그대로 유지하고, 탭 제어만 URL 파생으로 바꾼다:

```tsx
'use client'

import { Suspense } from 'react'
import { usePathname, useRouter, useSearchParams } from 'next/navigation'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { GuidelineViewer } from './guideline-viewer'
import { ChecklistPanel } from './checklist-panel'
import { MilestonePanel } from './milestone-panel'
import { IssuePanel } from './issue-panel'
import { FileList } from '@/components/drive/file-list'
import { computeProgress } from '@/lib/progress'
import type { Team, ChecklistItem, Milestone, Issue, DriveFile } from '@/lib/types/models'

const TAB_VALUES = ['overview', 'guideline', 'checklist', 'milestones', 'issues'] as const
type TabValue = (typeof TAB_VALUES)[number]

interface TeamTabsProps {
  team: Team
  checklist: ChecklistItem[]
  milestones: Milestone[]
  // ChecklistPanel은 다른 팀 소유 마일스톤 아래 배정된 항목까지 올바른 라벨을
  // 표시해야 하므로 전체 마일스톤을 받는다. MilestonePanel은 여전히 milestones
  // (팀 범위)를 사용한다.
  allMilestones: Milestone[]
  issues: Issue[]
  activityFeed?: never
  driveFiles: DriveFile[]
}

// useSearchParams는 정적 페이지(generateStaticParams)에서 Suspense 경계 필수.
export function TeamTabs(props: TeamTabsProps) {
  return (
    <Suspense fallback={null}>
      <TeamTabsInner {...props} />
    </Suspense>
  )
}

function TeamTabsInner({
  team,
  checklist,
  milestones,
  allMilestones,
  issues,
  driveFiles,
}: TeamTabsProps) {
  const searchParams = useSearchParams()
  const router = useRouter()
  const pathname = usePathname()
  const { completed, percent: progress } = computeProgress(checklist)

  // 탭 상태는 URL(?tab=)이 진실 원천 — 로컬 state 중복 없음. 무효 값은 개요 폴백.
  const tabParam = searchParams.get('tab')
  const value: TabValue = (TAB_VALUES as readonly string[]).includes(tabParam ?? '')
    ? (tabParam as TabValue)
    : 'overview'

  const handleTabChange = (next: string) => {
    // 수동 전환 시 ?focus= 제거 — 과거 포커스 항목으로 재스크롤 방지.
    // replace라 히스토리에 쌓이지 않고 scroll:false로 스크롤 점프도 없음.
    router.replace(`${pathname}?tab=${next}`, { scroll: false })
  }

  const focusItemId = searchParams.get('focus')

  return (
    <Tabs value={value} onValueChange={handleTabChange} className="w-full">
      <TabsList className="w-full overflow-x-auto">
        <TabsTrigger className="shrink-0" value="overview">개요</TabsTrigger>
        <TabsTrigger className="shrink-0" value="guideline">지침</TabsTrigger>
        <TabsTrigger className="shrink-0" value="checklist">
          체크리스트 ({completed}/{checklist.length})
        </TabsTrigger>
        <TabsTrigger className="shrink-0" value="milestones">마일스톤</TabsTrigger>
        <TabsTrigger className="shrink-0" value="issues">이슈 ({issues.length})</TabsTrigger>
      </TabsList>

      <TabsContent value="overview" className="mt-4 space-y-4">
        <div className="rounded-lg border p-4">
          <h3 className="mb-2 font-semibold">미션</h3>
          <p className="text-sm text-muted-foreground">{team.mission}</p>
        </div>
        <div className="rounded-lg border p-4">
          <div className="mb-2 flex items-center justify-between">
            <h3 className="font-semibold">진행률</h3>
            <span className="text-2xl font-bold" style={{ color: team.color }}>
              {progress}%
            </span>
          </div>
          <div className="h-2 w-full overflow-hidden rounded-full bg-muted">
            <div
              className="h-full rounded-full"
              style={{ width: `${progress}%`, backgroundColor: team.color }}
            />
          </div>
        </div>

        <FileList files={driveFiles} teamId={team.id} />
      </TabsContent>

      <TabsContent value="guideline" className="mt-4">
        <GuidelineViewer team={team} teamId={team.id} />
      </TabsContent>

      <TabsContent value="checklist" className="mt-4">
        <ChecklistPanel
          items={checklist}
          milestones={allMilestones}
          teamId={team.id}
          focusItemId={focusItemId}
        />
      </TabsContent>

      <TabsContent value="milestones" className="mt-4">
        <MilestonePanel milestones={milestones} />
      </TabsContent>

      <TabsContent value="issues" className="mt-4">
        <IssuePanel issues={issues} teamId={team.id} />
      </TabsContent>
    </Tabs>
  )
}
```

- [ ] **Step 2: 게이트 확인**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | tail -3`
Expected: 전부 통과

- [ ] **Step 3: 프로덕션 빌드 확인 (Suspense/useSearchParams 정적 렌더 제약 검증)**

Run: `npm run build 2>&1 | tail -15`
Expected: 빌드 성공 — 팀 페이지 5개 정적 생성, `useSearchParams` 관련 에러 없음.
(실패 시 에러 메시지에 Suspense 안내가 나오는지 확인 — 래퍼가 이미 있으므로 나오지 않아야 정상)

- [ ] **Step 4: 커밋**

```bash
git add sportsday-hub/components/team/team-tabs.tsx
git commit -m "feat: 팀 페이지 탭 URL 연동(?tab=) + focus 전달 — 딥링크 완성"
```

---

### Task 6: 수동 검증 + PR

**Files:**
- 없음 (검증·배포 태스크)

**Interfaces:**
- Consumes: Task 1~5 전부
- Produces: 머지 가능한 PR

- [ ] **Step 1: 개발 서버 기동**

Run: `npm run dev` (백그라운드)
Expected: `http://localhost:3000` 기동

- [ ] **Step 2: 수동 검증 시나리오 (스펙 §6)**

1. 대시보드 긴급 위젯 항목 클릭 → 해당 팀 페이지 **체크리스트 탭 열림** + URL이 `/team/{id}?tab=checklist&focus={itemId}` + 해당 항목이 **화면 중앙**으로 스크롤 + 3px 링 하이라이트
2. 하이라이트가 약 2.5초 후 **자연 소멸**, 이후 재노출 없음
3. 다른 팀의 위젯 항목 클릭 → 그 팀·항목으로 정상 동작
4. 탭 수동 전환(예: 마일스톤) → URL이 `?tab=milestones`로 갱신(focus 소멸) → 새로고침해도 마일스톤 탭 유지 + 재스크롤 없음
5. 주소창에 `?tab=foo`(무효값) 직접 입력 → 개요 탭 폴백
6. (엣지) `focus=존재하지않는ID` 직접 입력 → 체크리스트 탭만 열리고 아무 스크롤 없음 — 콘솔 에러 없음

- [ ] **Step 3: 최종 게이트**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | tail -3`
Expected: 전부 통과 (78/78)

- [ ] **Step 4: 푸시 + PR**

```bash
git push -u origin feat/긴급체크리스트-딥링크
```

PR 제목: `feat: 긴급 체크리스트 → 팀 체크리스트 딥링크 (스크롤+하이라이트)`
PR 본문: 스펙 링크 + 수동 검증 시나리오 결과 + "리뷰 서브에이전트 통과 후 머지" 명시

- [ ] **Step 5: 리뷰 서브에이전트 실행 → Critical/Important 없으면 머지**

- [ ] **Step 6: CI 통과 + Vercel 프로덕션 배포 READY 확인**

---

## Self-Review 기록

- **Spec 커버리지**: §4 컴포넌트 4개 + 신규 2파일 → Task 1(URL빌더)·2(CSS)·3(위젯)·4(패널)·5(탭). §5 엣지케이스 전부 Task 5·6 검증 항목에 대응. §6 수동 시나리오 5개 → Task 6 Step 2. ✅
- **Placeholder**: 없음 — 모든 스텝에 전체 코드/명령/기대값 명시. ✅
- **타입 일관성**: `buildChecklistFocusUrl(teamId: string | null | undefined, itemId: string): string | null` (Task 1 정의 = Task 3 사용). `focusItemId?: string | null` (Task 4 정의 = Task 5 전달). `.checklist-focus-flash` (Task 2 정의 = Task 4 사용). ✅
- **태스크 순서 독립성**: 각 태스크 종료 시점 게이트가 모두 green이 되도록 배치 (Task 3 링크는 아무 동작 없는 URL이지만 compile/test 무결). ✅
