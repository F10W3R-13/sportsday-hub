# 신원 체인 장착 + 죽은 쓰기 표면 제거 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모든 뮤테이션의 감사 기록이 닉네임을 반영하게 `ensureContext`를 관문화하고, 호출부 없는 익명 service role 쓰기 엔드포인트 2건(`save_folders`, `auto-map`)을 제거한다.

**Architecture:** 이미 모든 뮤테이션이 호출하는 `ensureContext`(`lib/supabase/client.ts`)에 닉네임 게이트를 내장한다. 게이트는 순수 판정 함수 `shouldPromptNickname` + window 커스텀 이벤트(`nickname:prompt`)로 provider와 통신하며, provider의 다이얼로그가 설정/건너뛰기로 해소한다. 컴포넌트·뮤테이션 훅 수정은 provider와 dialog뿐이다.

**Tech Stack:** Next.js App Router(전부 클라이언트 컴포넌트 대상 변경), Supabase anon 클라이언트, vitest(node 환경, `@/` alias).

**스펙:** `docs/superpowers/specs/2026-08-18-신원체인-장착-쓰기표면-제거-design.md` (커밋 `3b1c721`)

## Global Constraints

- 익명 열린 편집 철학 유지 — 인증·권한 도입 금지, 게이트는 강제 아님(건너뛰기 허용, '익명' 폴백 유지)
- UI 문구는 한국어, 기존 톤 유지
- Select는 @base-ui/react 래퍼(Radix 아님) — 본 계획에서 Select는 다루지 않음
- React Compiler lint가 **set-state-in-effect를 금지** — 이벤트 리스너 콜백 내 setState는 허용(이펙트 본문이 아니므로)이지만, 이펙트 본문에서 직접 setState 호출 금지
- DB 변경 없음(`set_user_context` RPC는 기존 것 사용) — 마이그레이션 불필요
- 테스트 러너: `vitest run`(node 환경 — `window`/`localStorage`는 `vi.stubGlobal`으로 주입)
- 모든 커밋은 `sportsday-hub/`가 아닌 **저장소 루트**(`26-2 스포츠데이기획/`)에서 실행 — 경로 prefix `sportsday-hub/` 명시

## File Structure

| 파일 | 역할 | 변경 |
|---|---|---|
| `sportsday-hub/lib/supabase/client.ts` | 신원 게이트(순수 판정 + 이벤트 발행 + ensureContext) | 수정 |
| `sportsday-hub/components/layout/nickname-provider.tsx` | 이벤트 수신 → 다이얼로그 호스팅, resolve 연결 | 수정 |
| `sportsday-hub/components/layout/nickname-dialog.tsx` | '건너뛰기' 버튼 + 문구 정정 | 수정 |
| `sportsday-hub/app/api/drive/sync/route.ts` | 동기화 전용으로 단순화 | 수정 |
| `sportsday-hub/app/api/drive/auto-map/route.ts` | 호출부 없는 죽은 쓰기 엔드포인트 | 삭제 |
| `sportsday-hub/tests/nickname-gate.test.ts` | 순수 판정 + 게이트 배선 테스트 | 신규 |
| `sportsday-hub/tests/api-drive-sync.test.ts` | save_folders 케이스·createServiceClient mock 제거 | 수정 |
| `sportsday-hub/tests/api-drive-auto-map.test.ts` | 죽은 라우트 테스트 | 삭제 |

---

### Task 1: 순수 판정 함수 `shouldPromptNickname`

**Files:**
- Modify: `sportsday-hub/lib/supabase/client.ts`
- Test: `sportsday-hub/tests/nickname-gate.test.ts` (신규)

**Interfaces:**
- Consumes: 없음 (기존 `getNickname`/`hasNickname`은 건드리지 않음)
- Produces: `shouldPromptNickname(input: NicknameGateInput): boolean`, `interface NicknameGateInput { hasNickname: boolean; promptedThisSession: boolean; providerReady: boolean }` — Task 2·3이 이 이름을 그대로 사용

- [ ] **Step 1: 실패 테스트 작성**

`sportsday-hub/tests/nickname-gate.test.ts` 생성:

```ts
import { describe, it, expect } from 'vitest'
import { shouldPromptNickname } from '@/lib/supabase/client'
import type { NicknameGateInput } from '@/lib/supabase/client'

describe('shouldPromptNickname (닉네임 게이트 순수 판정)', () => {
  it('provider 미준비(SSR·테스트 환경)면 절대 프롬프트하지 않는다', () => {
    const base: NicknameGateInput = {
      hasNickname: false,
      promptedThisSession: false,
      providerReady: false,
    }
    expect(shouldPromptNickname(base)).toBe(false)
    expect(shouldPromptNickname({ ...base, hasNickname: true })).toBe(false)
    expect(shouldPromptNickname({ ...base, promptedThisSession: true })).toBe(false)
  })

  it('닉네임 없음 + 세션 내 미프롬프트 + provider 준비됨일 때만 프롬프트', () => {
    const input: NicknameGateInput = {
      hasNickname: false,
      promptedThisSession: false,
      providerReady: true,
    }
    expect(shouldPromptNickname(input)).toBe(true)
  })

  it('닉네임이 있으면 프롬프트하지 않는다', () => {
    const input: NicknameGateInput = {
      hasNickname: true,
      promptedThisSession: false,
      providerReady: true,
    }
    expect(shouldPromptNickname(input)).toBe(false)
  })

  it('세션 내 이미 프롬프트했으면 다시 하지 않는다', () => {
    const input: NicknameGateInput = {
      hasNickname: false,
      promptedThisSession: true,
      providerReady: true,
    }
    expect(shouldPromptNickname(input)).toBe(false)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd sportsday-hub && npx vitest run tests/nickname-gate.test.ts`
Expected: FAIL — `No matching export` (아직 `shouldPromptNickname`이 없음)

- [ ] **Step 3: 최소 구현**

`sportsday-hub/lib/supabase/client.ts`에 추가 (기존 내용 유지, 파일 상단 import 아래쪽 적절한 위치):

```ts
export interface NicknameGateInput {
  hasNickname: boolean
  promptedThisSession: boolean
  providerReady: boolean
}

// 닉네임 게이트 순수 판정 — provider 준비 + 닉네임 부재 + 세션 내 미프롬프트일 때만 프롬프트.
// SSR은 providerReady=false로 자연 처리된다.
export function shouldPromptNickname(input: NicknameGateInput): boolean {
  return input.providerReady && !input.hasNickname && !input.promptedThisSession
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd sportsday-hub && npx vitest run tests/nickname-gate.test.ts`
Expected: PASS (4개)

- [ ] **Step 5: 커밋**

```bash
git add sportsday-hub/lib/supabase/client.ts sportsday-hub/tests/nickname-gate.test.ts
git commit -m "feat: shouldPromptNickname 순수 판정 함수 — 닉네임 게이트 기초"
```

---

### Task 2: `ensureContext` 관문화 + 이벤트 배선

**Files:**
- Modify: `sportsday-hub/lib/supabase/client.ts`
- Test: `sportsday-hub/tests/nickname-gate.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: Task 1의 `shouldPromptNickname`, 기존 `getNickname`/`hasNickname`
- Produces:
  - `NICKNAME_PROMPT_EVENT = 'nickname:prompt'` (상수)
  - `interface NicknamePromptDetail { resolve: () => void }`
  - `requestNicknameViaProvider(): Promise<void>` — provider 이벤트 발행 후 resolve 대기
  - 갱신된 `ensureContext(client: ReturnType<typeof createClient>): Promise<void>` (기존 시그니처 동일 — 모든 뮤테이션 훅 수정 불필요)
  - provider 준비 플래그: `window.__sportsdayNicknameGateReady === true` (Task 3이 설정)
  - 세션 마킹 키: `'sportsday-nickname-prompted'`

- [ ] **Step 1: 실패 테스트 추가**

`tests/nickname-gate.test.ts`에 import 확장 및 케이스 추가:

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import {
  shouldPromptNickname,
  ensureContext,
  requestNicknameViaProvider,
  NICKNAME_PROMPT_EVENT,
  type NicknameGateInput,
  type NicknamePromptDetail,
} from '@/lib/supabase/client'

// --- 기존 shouldPromptNickname describe 그대로 유지 ---

describe('ensureContext 닉네임 게이트 배선', () => {
  beforeEach(() => {
    vi.unstubAllGlobals()
  })
  afterEach(() => {
    vi.unstubAllGlobals()
  })

  function stubStorage() {
    const local = new Map<string, string>()
    const session = new Map<string, string>()
    vi.stubGlobal('localStorage', {
      getItem: (k: string) => local.get(k) ?? null,
      setItem: (k: string, v: string) => local.set(k, v),
    })
    vi.stubGlobal('sessionStorage', {
      getItem: (k: string) => session.get(k) ?? null,
      setItem: (k: string, v: string) => session.set(k, v),
    })
    return { local, session }
  }

  function stubWindow(options: { ready: boolean }) {
    const listeners = new Map<string, Set<(e: Event) => void>>()
    const win = {
      __sportsdayNicknameGateReady: options.ready,
      addEventListener: (type: string, fn: (e: Event) => void) => {
        if (!listeners.has(type)) listeners.set(type, new Set())
        listeners.get(type)!.add(fn)
      },
      removeEventListener: (type: string, fn: (e: Event) => void) => {
        listeners.get(type)?.delete(fn)
      },
      dispatchEvent: (event: Event) => {
        listeners.get(event.type)?.forEach((fn) => fn(event))
        return true
      },
    }
    vi.stubGlobal('window', win)
    return win
  }

  const fakeClient = () => ({ rpc: vi.fn().mockResolvedValue(undefined) })

  it('provider 미준비면 프롬프트 없이 익명으로 진행 (데드락 방지)', async () => {
    const { session } = stubStorage()
    stubWindow({ ready: false })
    const client = fakeClient()

    await ensureContext(client as never)

    expect(client.rpc).toHaveBeenCalledWith('set_user_context', { p_nickname: '익명' })
    expect(session.has('sportsday-nickname-prompted')).toBe(false)
  })

  it('닉네임 있으면 프롬프트 없이 닉네임으로 진행', async () => {
    const { local } = stubStorage()
    local.set('sportsday-nickname', '지훈')
    stubWindow({ ready: true })
    const client = fakeClient()

    await ensureContext(client as never)

    expect(client.rpc).toHaveBeenCalledWith('set_user_context', { p_nickname: '지훈' })
  })

  it('닉네임 없으면 이벤트 발행 → 설정 해소 대기 → 세션 마킹', async () => {
    const { local, session } = stubStorage()
    const win = stubWindow({ ready: true })
    win.addEventListener(NICKNAME_PROMPT_EVENT, (e) => {
      const detail = (e as CustomEvent<NicknamePromptDetail>).detail
      local.set('sportsday-nickname', '민수')
      detail.resolve()
    })
    const client = fakeClient()

    await ensureContext(client as never)

    expect(client.rpc).toHaveBeenCalledWith('set_user_context', { p_nickname: '민수' })
    expect(session.get('sportsday-nickname-prompted')).toBe('1')
  })

  it('건너뛰기(resolve만)도 세션 마킹 후 익명으로 진행', async () => {
    const { session } = stubStorage()
    const win = stubWindow({ ready: true })
    win.addEventListener(NICKNAME_PROMPT_EVENT, (e) => {
      ;(e as CustomEvent<NicknamePromptDetail>).detail.resolve()
    })
    const client = fakeClient()

    await ensureContext(client as never)

    expect(client.rpc).toHaveBeenCalledWith('set_user_context', { p_nickname: '익명' })
    expect(session.get('sportsday-nickname-prompted')).toBe('1')
  })

  it('대기 중 추가 호출은 같은 Promise를 재사용', async () => {
    stubStorage()
    const win = stubWindow({ ready: true })
    let release: (() => void) | null = null
    win.addEventListener(NICKNAME_PROMPT_EVENT, (e) => {
      release = (e as CustomEvent<NicknamePromptDetail>).detail.resolve
    })

    const first = requestNicknameViaProvider()
    const second = requestNicknameViaProvider()
    expect(first).toBe(second)

    release!()
    await first
  })
})
```

주의: node 환경 `CustomEvent`는 전역 존재(Node 19+). 구현부에서 `new CustomEvent<NicknamePromptDetail>(...)`를 사용하므로 테스트의 `(e as CustomEvent<NicknamePromptDetail>)` 캐스트와 정합.

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd sportsday-hub && npx vitest run tests/nickname-gate.test.ts`
Expected: FAIL — `NICKNAME_PROMPT_EVENT`/`requestNicknameViaProvider`/새 `ensureContext` 동작 없음 (import 실패 또는 기대 불일치)

- [ ] **Step 3: 구현**

`lib/supabase/client.ts` — Task 1에서 추가한 코드 위에 아래를 추가하고, 기존 `ensureContext`를 교체:

```ts
declare global {
  interface Window {
    __sportsdayNicknameGateReady?: boolean
  }
}

export const NICKNAME_PROMPT_EVENT = 'nickname:prompt'
const NICKNAME_PROMPTED_KEY = 'sportsday-nickname-prompted'

export interface NicknamePromptDetail {
  resolve: () => void
}

function isProviderReady(): boolean {
  return typeof window !== 'undefined' && window.__sportsdayNicknameGateReady === true
}

// sessionStorage 접근 불가 환경(일부 프라이버시 모드)은 프롬프트 생략 — 마찰 최소 정책
function wasPromptedThisSession(): boolean {
  try {
    return sessionStorage.getItem(NICKNAME_PROMPTED_KEY) === '1'
  } catch {
    return true
  }
}

function markPromptedThisSession(): void {
  try {
    sessionStorage.setItem(NICKNAME_PROMPTED_KEY, '1')
  } catch {
    /* no-op */
  }
}

// 대기 중 중복 호출은 동일 Promise 재사용 (이중 다이얼로그 방지)
let pendingPrompt: Promise<void> | null = null

export function requestNicknameViaProvider(): Promise<void> {
  if (pendingPrompt) return pendingPrompt
  if (!isProviderReady()) return Promise.resolve()
  pendingPrompt = new Promise<void>((resolve) => {
    window.dispatchEvent(
      new CustomEvent<NicknamePromptDetail>(NICKNAME_PROMPT_EVENT, {
        detail: {
          resolve: () => {
            pendingPrompt = null
            resolve()
          },
        },
      })
    )
  })
  return pendingPrompt
}

// 편집 전 호출 — 닉네임 게이트 통과 후 세션 변수 주입해 audit 트리거가 읽음
export async function ensureContext(
  client: ReturnType<typeof createClient>
): Promise<void> {
  if (
    shouldPromptNickname({
      hasNickname: hasNickname(),
      promptedThisSession: wasPromptedThisSession(),
      providerReady: isProviderReady(),
    })
  ) {
    await requestNicknameViaProvider()
    markPromptedThisSession()
  }
  const nickname = getNickname() ?? '익명'
  await client.rpc('set_user_context', { p_nickname: nickname })
}
```

기존 `ensureContext`(구 버전)는 삭제 — 같은 이름 교체.

- [ ] **Step 4: 테스트 통과 + 타입체크**

Run: `cd sportsday-hub && npx vitest run tests/nickname-gate.test.ts && npm run typecheck`
Expected: PASS (순수 4 + 배선 5), tsc 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add sportsday-hub/lib/supabase/client.ts sportsday-hub/tests/nickname-gate.test.ts
git commit -m "feat: ensureContext 닉네임 게이트 관문화 — 프롬프트 이벤트·세션 마킹·데드락 방지"
```

---

### Task 3: provider 이벤트 수신 + 다이얼로그 건너뛰기

**Files:**
- Modify: `sportsday-hub/components/layout/nickname-provider.tsx` (전면 교체)
- Modify: `sportsday-hub/components/layout/nickname-dialog.tsx`

**Interfaces:**
- Consumes: Task 2의 `NICKNAME_PROMPT_EVENT`, `NicknamePromptDetail`
- Produces: 없음 (외부 인터페이스 변화 없음 — 호출자 없는 `useNicknamePrompt` export는 이번에 삭제)

- [ ] **Step 1: provider 교체**

`components/layout/nickname-provider.tsx` 전체를 아래로 교체. 컨텍스트/`useNicknamePrompt`는 호출자가 없으므로 제거(죽은 코드 정리 — 본 세션 주제와 동일한 규율):

```tsx
'use client'

import { useState, useCallback, useEffect, useRef } from 'react'
import { NicknameDialog } from './nickname-dialog'
import {
  NICKNAME_PROMPT_EVENT,
  type NicknamePromptDetail,
} from '@/lib/supabase/client'

export function NicknameProvider({ children }: { children: React.ReactNode }) {
  const [showDialog, setShowDialog] = useState(false)
  const resolveRef = useRef<(() => void) | null>(null)

  // 게이트(lib/supabase/client.ts)가 발행하는 이벤트 수신.
  // setState는 이벤트 콜백 안에서 호출 — React Compiler의 set-state-in-effect
  // 규칙 대상이 아니다(이펙트 본문 아님).
  useEffect(() => {
    const handlePrompt = (e: Event) => {
      const detail = (e as CustomEvent<NicknamePromptDetail>).detail
      resolveRef.current = detail.resolve
      setShowDialog(true)
    }
    window.addEventListener(NICKNAME_PROMPT_EVENT, handlePrompt)
    window.__sportsdayNicknameGateReady = true
    return () => {
      window.__sportsdayNicknameGateReady = false
      window.removeEventListener(NICKNAME_PROMPT_EVENT, handlePrompt)
    }
  }, [])

  // 닫기는 설정·건너뛰기·Escape 모두 동일 취급 — 대기 중인 편집을 해소한다
  const handleClose = useCallback(() => {
    setShowDialog(false)
    const resolve = resolveRef.current
    resolveRef.current = null
    resolve?.()
  }, [])

  return (
    <>
      {children}
      <NicknameDialog open={showDialog} onClose={handleClose} />
    </>
  )
}
```

- [ ] **Step 2: 다이얼로그에 건너뛰기 버튼 + 문구 정정**

`components/layout/nickname-dialog.tsx`에서 두 곳 수정.

`DialogDescription` 문구 — "나중에 언제든 변경할 수 있습니다"는 현재 사실이 아니므로(변경 진입구 없음) 정정:

```tsx
<DialogDescription>
  편집 시 누가 변경했는지 기록하는 데 사용됩니다. 건너뛰면 '익명'으로 기록돼요.
</DialogDescription>
```

`DialogFooter` — 건너뛰기 보조 버튼 추가:

```tsx
<DialogFooter>
  <Button variant="outline" onClick={onClose}>
    건너뛰기
  </Button>
  <Button onClick={handleSave} disabled={!value.trim()}>
    저장
  </Button>
</DialogFooter>
```

(변경 없음: `handleSave`의 `setNickname` → `onClose` 흐름 그대로 — provider의 `handleClose`가 resolve 호출)

- [ ] **Step 3: 타입체크 + lint + 전체 테스트**

Run: `cd sportsday-hub && npm run typecheck && npm run lint && npx vitest run`
Expected: 전부 통과 (`useNicknamePrompt` import 잔여 없음 확인 — 호출자 0개였으나 만일 다른 파일이 import 중이면 typecheck가 잡음)

- [ ] **Step 4: 수동 스모크 (로컬 dev)**

Run: `cd sportsday-hub && npm run dev` 후 시크릿 브라우저(또는 DevTools에서 localStorage `sportsday-nickname`·sessionStorage `sportsday-nickname-prompted` 삭제)로:
1. 체크리스트 체크 → 닉네임 다이얼로그 등장
2. '건너뛰기' → 편집 완료 ('익명' 기록 — Supabase `audit_log.changed_by`에서 사용자 확인)
3. 같은 세션에서 재편집 → 재프롬프트 없음
4. 새 시크릿 창 → 닉네임 입력 저장 → 편집 완료 (`changed_by`에 닉네임)
5. Escape로 닫기도 편집 진행되는지 확인

Expected: 1–5 모두 정상. 실패 시 Task 2·3의 이벤트 배선 재점검.

- [ ] **Step 5: 커밋**

```bash
git add sportsday-hub/components/layout/nickname-provider.tsx sportsday-hub/components/layout/nickname-dialog.tsx
git commit -m "feat: 닉네임 다이얼로그 게이트 장착 — 이벤트 수신·건너뛰기·문구 정정"
```

---

### Task 4: 죽은 쓰기 표면 제거

**Files:**
- Modify: `sportsday-hub/app/api/drive/sync/route.ts` (전면 교체)
- Delete: `sportsday-hub/app/api/drive/auto-map/route.ts`
- Modify: `sportsday-hub/tests/api-drive-sync.test.ts`
- Delete: `sportsday-hub/tests/api-drive-auto-map.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `/api/drive/sync` POST 바디는 `{ teamId?, force? }`만 유효 (save_folders 액션 제거 — 문서화 목)

- [ ] **Step 1: sync 라우트 단순화**

`app/api/drive/sync/route.ts` 전체 교체 — `save_folders` 브랜치와 `createServiceClient` import 제거:

```ts
import { NextRequest, NextResponse } from 'next/server'
import { syncDriveFiles } from '@/lib/drive/sync'
import type { TeamId } from '@/lib/types/models'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json().catch(() => ({}))

    const teamId = body.teamId as TeamId | undefined
    const force = body.force === true

    const result = await syncDriveFiles(teamId, force)

    if (!result.success && result.error === 'not_connected') {
      return NextResponse.json({ error: 'drive_not_connected' }, { status: 401 })
    }

    return NextResponse.json(result)
  } catch (err) {
    console.error('Sync error:', err)
    return NextResponse.json({ error: 'sync_failed' }, { status: 500 })
  }
}
```

- [ ] **Step 2: sync 테스트에서 save_folders 케이스 제거**

`tests/api-drive-sync.test.ts` — import에서 `createServiceClient` 제거, mock 팩토리에서도 제거:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import type { NextRequest } from 'next/server'
import { POST } from '@/app/api/drive/sync/route'
import { syncDriveFiles } from '@/lib/drive/sync'

vi.mock('@/lib/drive/sync', () => ({
  syncDriveFiles: vi.fn(),
}))
```

이후 `'save_folders 액션 — 매핑 저장 후 강제 동기화'` it 블록(40–59행) 전체 삭제. 나머지 5개 케이스는 변경 없음. `jsonRequest` 헬퍼도 유지.

- [ ] **Step 3: auto-map 라우트·테스트 삭제**

```bash
git rm sportsday-hub/app/api/drive/auto-map/route.ts sportsday-hub/tests/api-drive-auto-map.test.ts
```

(`discoverTeamFolders`는 `google-callback` 라우트가 사용하므로 `lib/drive/sync.ts`는 건드리지 않는다)

- [ ] **Step 4: 전체 테스트·타입체크·lint**

Run: `cd sportsday-hub && npx vitest run && npm run typecheck && npm run lint`
Expected: 전부 통과 (총 테스트 수에서 auto-map 파일·save_folders 케이스 감소분 반영)

- [ ] **Step 5: 커밋**

```bash
git add -A sportsday-hub/app/api sportsday-hub/tests
git commit -m "refactor: 죽은 쓰기 표면 제거 — save_folders 액션·auto-map 라우트 (호출부 없는 익명 service role 쓰기)"
```

---

### Task 5: 최종 검증 (배포 게이트)

**Files:** 변경 없음 — 검증만

- [ ] **Step 1: 전체 회귀**

Run: `cd sportsday-hub && npx vitest run && npm run typecheck && npm run lint && npm run build`
Expected: 전부 통과 (build는 Vercel 배포 전 로컬 확인)

- [ ] **Step 2: 프로덕션 배포 후 스모크 (사용자 협조)**

main 푸시 = 즉시 배포. DB 변경 없으므로 마이그레이션 불필요. 배포 후:
1. 시크릿 창에서 체크리스트 편집 → 프롬프트 → 닉네임 설정 → Supabase `audit_log.changed_by` 확인
2. `/files`·`/handoffs`·대시보드 정상 로드 (기존 회귀)
3. `/api/drive/sync` 수동 동기화 버튼 정상 (auto-map·save_folders 제거 영향 없음 확인)

- [ ] **Step 3: 회고록 액션 아이템 상태 갱신**

`docs/lessons/2026-08-18-팀간-파일공유-1-2단계-회고.md`의 액션 아이템 표에서 #3·#4를 완료 처리(각각 "죽은 코드 제거로 해소", "ensureContext 관문화로 장착")하고 커밋:

```bash
git add "docs/lessons/2026-08-18-팀간-파일공유-1-2단계-회고.md"
git commit -m "docs: 회고 액션 아이템 #3·#4 완료 처리 — 실상(죽은 코드·미장착) 반영"
```
