# 소관 월권 방어 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 26-2 2차 회의 "교환·국제처" 월권 사건을 회수하고, 소관 매핑 진본 + 검증 스크립트 + 전사문 워크플로우로 재발을 구조적으로 방어한다.

**Architecture:** 마스터 지침에 YAML 소관 매핑(단일 진본)을 두고, 검증 스크립트가 회의/사전공유 문서의 "담당" 할당을 매핑과 대조한다. 순수 로직은 테스트 가능한 lib 모듈로 분리하고, CLI는 얇게 감싼다. 매핑 유지보수는 회의 전사문 → AI 분석 → 승인 게이트 루프로 처리한다.

**Tech Stack:** TypeScript, tsx (스크립트 실행), vitest (테스트), js-yaml (매핑 파싱), 마크다운 문서 (대상).

**Spec:** `docs/superpowers/specs/2026-08-09-소관-월권-방어-design.md`

## Global Constraints

- 문서 인코딩: 모든 마크다운은 UTF-8, 줄바꿈 LF (git이 CRLF 변환 경고해도 무방).
- 팀명은 한국어 그대로 사용 (영문 key 변환 계층 없음): 기획관리팀, 컨텐츠팀, 예산팀, 교환담당팀, 타임라인/인원관리팀.
- 외부 컨택 키워드(국제처, 총학, 시설관리팀, 동아리 보전)는 5팀 어디에도 할당되면 안 된다.
- 모든 스크립트는 `sportsday-hub/` 디렉토리에서 실행한다 (`npx tsx scripts/...`).
- 기존 패턴 준수: `verify-guideline-sync.ts`와 동일한 구조/출력 스타일.

## File Structure

| 파일 | 책임 | 상태 |
|---|---|---|
| `26-2 Sports Day/2차 회의 안건서 (진행용).md` | 1-C 블록 명명 정정 + 국제처 보고 제거 | Modify |
| `26-2 Sports Day/교환담당팀/2차 회의 사전 공유 (교환담당팀용).md` | 국제처 섹션 전면 삭제 | Modify |
| `26-2 Sports Day/00_기획지침_마스터.md` | 소관 매핑 YAML + 작성 원칙 2개 추가 | Modify |
| `sportsday-hub/content-source/00_기획지침_마스터.md` | 위와 동기화 | Modify |
| `26-2 Sports Day/회의록/.gitkeep` | 전사문 아카이브 폴더 | Create |
| `sportsday-hub/scripts/lib/scope-mapping.ts` | 순수 로직: 매핑 파싱·문서 검사 | Create |
| `tests/scope-mapping.test.ts` | scope-mapping lib 단위 테스트 | Create |
| `sportsday-hub/scripts/verify-scope-ownership.ts` | CLI: 파일 읽고 lib 호출·출력·exit | Create |
| `sportsday-hub/tests/fixtures/scope/` | 검증 픽스처 (정상/위반 샘플 문서) | Create |
| `sportsday-hub/package.json` | js-yaml devDep + `verify` 스크립트 | Modify |

---

### Task 1: 틀린 회의 문서 바로잡기 (안건서 + 사전공유)

**Files:**
- Modify: `26-2 Sports Day/2차 회의 안건서 (진행용).md` (1-C 블록, ~115-132행)
- Modify: `26-2 Sports Day/교환담당팀/2차 회의 사전 공유 (교환담당팀용).md` (국제처 섹션 전체)

**Interfaces:**
- Consumes: 없음 (독립 내용 수정)
- Produces: 국제처가 교환담당팀에 할당된 모든 텍스트 제거 (이후 Task 5 검증의 baseline)

- [ ] **Step 1: 안건서 1-C 블록 명명 정정**

`26-2 Sports Day/2차 회의 안건서 (진행용).md`에서 다음을 찾아 교체:

```markdown
### 1-C. 교환·국제처 (보고 10분)
```
→
```markdown
### 1-C. 교환담당팀 (보고 10분)
```

같은 블록의 보고 란에서:

```markdown
> - ⚪ 보고: 구글폼 진행 상황 (8/20 완성 목표) / 국제처 컨택 사항 (임원진 경유)
```
→
```markdown
> - ⚪ 보고: 구글폼 진행 상황 (8/20 완성 목표)
> - ℹ️ 국제처 컨택(천막/First Aid/대여물품/우천)은 교환담당팀 소관이 아님 — 임원진/기관팀 경유, 기획관리팀이 착수. 상세는 9월 초.
```

기록란의 `국제처 (천막/First Aid/대여물품/우천):` 줄은 그대로 두되(회의 진행용 빈 기록 칸), 담당이 교환담당팀으로 묶이지 않도록 블록명이 정정됐으므로 OK.

- [ ] **Step 2: 사전공유(교환담당팀) 국제처 전면 삭제**

`26-2 Sports Day/교환담당팀/2차 회의 사전 공유 (교환담당팀용).md`에서:

(a) "회의 전까지 할 것" 섹션 — "2. 국제처 컨택 사항 정리" 전체(14-17행) 삭제. 기존 "3. 발표 담당 1명 지정"을 2번으로 끌어올림:

삭제 대상:
```markdown
### 2. 국제처 컨택 사항 정리 (있는 경우)
- 천막 / First Aid / 대여물품 / 우천 대응
- 임원진 경유 컨택이라 시간이 걸리므로, 착수 여부만 공유하면 됩니다.
- **왜**: 이 항목들은 행사에 필수인데, 처리 방식(국제처 지원 vs 자체 예산)에 따라 예산 구조가 달라집니다. 다만 상세 확정은 9월 초 구간이므로, 지금은 착수 여부만 확인합니다.

### 3. 발표 담당 1명 지정
```
→
```markdown
### 2. 발표 담당 1명 지정
```

(b) "회의에서 발표할 것" 섹션 — "국제처 컨택 (보고)" 항목 삭제:

삭제 대상:
```markdown
### 국제처 컨택 (보고)
- 착수 여부, 진행 중인 사항

### 안내 — 구글폼 성별 항목
```
→
```markdown
### 안내 — 구글폼 성별 항목
```

(c) "배경" 섹션 — 국제처 항목 삭제:

삭제 대상 (마지막 불릿):
```markdown
- **국제처**: 천막·구급·우천 대응은 행사 안전에 직결됩니다. 임원진을 거쳐야 해서 리드타임이 길어, 일찍 착수해야 합니다.
```
(이 불릿만 제거, 앞의 구글폼·성별 항목 불릿은 유지)

- [ ] **Step 3: 회의록 아카이브 폴더 생성**

```bash
mkdir -p "26-2 Sports Day/회의록"
touch "26-2 Sports Day/회의록/.gitkeep"
```

- [ ] **Step 4: 수정 결과 검증 (grep)**

Run:
```bash
grep -c "국제처" "26-2 Sports Day/교환담당팀/2차 회의 사전 공유 (교환담당팀용).md"
grep -n "교환·국제처" "26-2 Sports Day/2차 회의 안건서 (진행용).md"
```
Expected: 첫 명령 `0` (국제처 완전 제거). 둘째 명령 출력 없음 (블록명 정정됨).

안건서엔 국제처가 안내문 한 줄(ℹ️)로 남아있어야 함:
```bash
grep -c "국제처" "26-2 Sports Day/2차 회의 안건서 (진행용).md"
```
Expected: `1` 이상 (ℹ️ 안내 줄). 이건 의도적 — 소관 아님을 명시하는 방어 문구.

- [ ] **Step 5: 커밋**

```bash
git add "26-2 Sports Day/2차 회의 안건서 (진행용).md" \
        "26-2 Sports Day/교환담당팀/2차 회의 사전 공유 (교환담당팀용).md" \
        "26-2 Sports Day/회의록/.gitkeep"
git commit -m "fix(meeting): 교환담당팀에 국제처 월권 할당 회수

안건서 1-C 블록명 '교환·국제처' → '교환담당팀' 정정.
사전공유(교환담당팀용)에서 국제처 컨택 섹션·보고·배경 전면 삭제.
국제처 컨택은 임원진/기관팀 경유 영역 (기획관리팀 착수)."
```

---

### Task 2: 마스터 지침에 소관 매핑 + 작성 원칙 추가

**Files:**
- Modify: `26-2 Sports Day/00_기획지침_마스터.md` (말미에 섹션 2개 추가)
- Modify: `sportsday-hub/content-source/00_기획지침_마스터.md` (동기화)

**Interfaces:**
- Consumes: Task 1 완료 (문서 구조 안정)
- Produces: 마스터 내 YAML 매핑 블록 — Task 3/4의 `extractMappingYaml` 파싱 대상. 매핑 블록은 `## 🔐 소관 매핑` 제목 아래 첫 번째 ` ```yaml ` 펜스.

- [ ] **Step 1: 마스터 말미에 소관 매핑 블록 추가**

`26-2 Sports Day/00_기획지침_마스터.md` 끝(67행 "지침은 원칙만..." 원칙 이후)에 추가. **정확히 이 YAML 구조를 유지할 것** (Task 3 파서가 이 형식에 의존):

```markdown

---

## 🔐 소관 매핑 (단일 진본)

> 각 팀이 직접 수행하는 업무의 키워드 매핑.
> 회의/사전공유/안건서의 "담당" 항목은 이 매핑과 일치해야 한다.
> 매핑 갱신은 회의 전사문 기반 (회의록/ 폴더 근거).
> 검증: \`npx tsx scripts/verify-scope-ownership.ts\`

\`\`\`yaml
# 5개 스포츠데이 기획 팀의 직접 소관
teams:
  기획관리팀:
    owns:
      - 기획 총괄
      - 팀 간 조율
      - 최종기획안 작성
  컨텐츠팀:
    owns:
      - 게임 구성
      - 게임 규칙
      - 배치도
      - 게임별 필요 인원
      - 게임 물품 리스트
  예산팀:
    owns:
      - 예산안 작성
      - 입장료 책정
      - 식사
      - 간식
      - 단체티
      - 준비물 리스트
  교환담당팀:
    owns:
      - 구글폼
      - 참여자 명단
      - 교환 팀 배정
      - 카드뉴스
      - 교환학생 정보방
  타임라인/인원관리팀:
    owns:
      - 타임라인
      - 하클 인원 배치
      - 명륜 버스
      - 귀환 셔틀

# 외부 컨택 — 5팀 소관 아님 (임원진/기관팀 경유)
external_contacts:
  대상:
    - 국제처
    - 총학
    - 시설관리팀
    - 동아리 보전
  실행주체: 임원진(기관팀) 공식계정 경유
\`\`\`
```

- [ ] **Step 2: 작성 원칙 2개 추가**

같은 파일, 소관 매핑 블록 다음에 추가:

```markdown

---

## 📝 회의/사전공유 문서 작성 원칙

### 🎯 담당 항목은 소관 매핑과 대조한다

**원칙.** 회의 안건서·사전공유·안내문의 "담당"/"준비"/"발표 담당" 항목은 작성 전 [소관 매핑](#-소관-매핑-단일-진본)과 대조한다. 매핑에 없는 업무를 한 팀에 할당하면 안 된다.

왜: 26-2 2차 회의에서 "교환·국제처" 블록 통합으로 교환담당팀에 국제처 컨택이 월권 할당된 사건이 있었다. 빈 공간이 매핑 대조 없이 가까운 팀으로 떨어진 것이다.
검증: \`npx tsx scripts/verify-scope-ownership.ts\`

### 🎯 회의 블록은 단일 팀 원칙

**원칙.** 회의 안건서의 발표 블록 이름은 단일 팀명으로 한다. "교환·국제처"처럼 복수 안건을 한 블록으로 묶되 팀명을 병기하지 않는다. 발표 시간 압축이 필요하면 같은 팀의 안건끼리만 묶는다.

왜: 블록명에 복수 안건을 병기하면 "담당도 병기됐다"로 오해된다. 이번 사건의 직접적 원인이 "교환·국제처" 명명이었다.
```

- [ ] **Step 3: content-source 동기화**

```bash
cp "26-2 Sports Day/00_기획지침_마스터.md" "sportsday-hub/content-source/00_기획지침_마스터.md"
```

- [ ] **Step 4: 동기화 검증**

Run:
```bash
cd sportsday-hub && npx tsx scripts/verify-guideline-sync.ts
```
Expected: `✅ 마스터: 동기화됨` 포함, exit 0.

- [ ] **Step 5: 매핑 블록 존재 확인**

Run:
```bash
grep -c "소관 매핑" "26-2 Sports Day/00_기획지침_마스터.md"
grep -c "external_contacts" "26-2 Sports Day/00_기획지침_마스터.md"
```
Expected: 둘 다 `1`.

- [ ] **Step 6: 커밋**

```bash
git add "26-2 Sports Day/00_기획지침_마스터.md" \
        "sportsday-hub/content-source/00_기획지침_마스터.md"
git commit -m "docs(guideline): 소관 매핑 YAML + 회의 문서 작성 원칙 추가

마스터 지침에 5팀 직접 소관 + 외부 컨택(5팀 밖) 매핑을 단일 진본으로 등록.
빈 공간 제로 — 국제처/총학/시설관리팀/동아리보전은 external_contacts로 명시.
회의 블록 단일 팀 원칙 + 담당 항목 매핑 대조 원칙으로 재발 방어."
```

---

### Task 3: js-yaml devDep 추가 + scope-mapping lib (TDD)

**Files:**
- Modify: `sportsday-hub/package.json` (js-yaml devDep)
- Create: `sportsday-hub/scripts/lib/scope-mapping.ts`
- Test: `sportsday-hub/tests/scope-mapping.test.ts`

**Interfaces:**
- Consumes: Task 2의 마스터 매핑 YAML 블록 형식
- Produces: `ScopeMapping` 타입, `extractMappingYaml`, `parseMapping`, `inferDocTeam`, `findExternalMentions`, `checkDoc`, `Violation` 타입 — Task 4 CLI가 소비.

- [ ] **Step 1: js-yaml devDep 추가**

`sportsday-hub/package.json`의 `devDependencies`에 추가 (알파벳 순 유지, ts 아래):

```json
    "js-yaml": "^4.1.0",
```

설치:
```bash
cd sportsday-hub && npm install
```

- [ ] **Step 2: 타입과 더미 내보내기로 실패 테스트 작성**

`sportsday-hub/tests/scope-mapping.test.ts` 생성:

```typescript
import { describe, expect, it } from 'vitest'
import {
  extractMappingYaml,
  parseMapping,
  inferDocTeam,
  findExternalMentions,
  checkDoc,
  type ScopeMapping,
} from '../scripts/lib/scope-mapping'

const SAMPLE_MASTER = `# 마스터

## 🔐 소관 매핑 (단일 진본)

\`\`\`yaml
teams:
  교환담당팀:
    owns:
      - 구글폼
      - 카드뉴스
external_contacts:
  대상:
    - 국제처
    - 총학
  실행주체: 임원진 경유
\`\`\`
`

describe('extractMappingYaml', () => {
  it('소관 매핑 제목 아래 yaml 펜스 내용을 추출한다', () => {
    const yaml = extractMappingYaml(SAMPLE_MASTER)
    expect(yaml).toContain('teams:')
    expect(yaml).toContain('국제처')
    expect(yaml).toContain('external_contacts:')
  })

  it('매핑 섹션이 없으면 빈 문자열', () => {
    expect(extractMappingYaml('# 그냥 문서\n본문')).toBe('')
  })
})

describe('parseMapping', () => {
  it('YAML을 ScopeMapping으로 파싱한다', () => {
    const yaml = extractMappingYaml(SAMPLE_MASTER)
    const mapping = parseMapping(yaml)
    expect(mapping.teams['교환담당팀'].owns).toEqual(['구글폼', '카드뉴스'])
    expect(mapping.external).toEqual(['국제처', '총학'])
  })
})

describe('inferDocTeam', () => {
  it('파일명의 (X팀용) 에서 팀 추론', () => {
    expect(inferDocTeam('교환담당팀/2차 회의 사전 공유 (교환담당팀용).md')).toBe('교환담당팀')
  })
  it('폴더명에서 팀 추론', () => {
    expect(inferDocTeam('예산팀/예산팀_지침.md')).toBe('예산팀')
  })
  it('팀 소속 아니면 null', () => {
    expect(inferDocTeam('2차 회의 안건서 (진행용).md')).toBeNull()
  })
  it('타임라인 별칭', () => {
    expect(inferDocTeam('타임라인_인원관리팀/x.md')).toBe('타임라인/인원관리팀')
  })
})

describe('findExternalMentions', () => {
  const mapping: ScopeMapping = {
    teams: { '교환담당팀': { owns: ['구글폼'] } },
    external: ['국제처', '총학'],
  }
  it('외부 키워드 등장 위치 반환', () => {
    const text = '구글폼 제작\n국제처 컨택 사항\n'
    const hits = findExternalMentions(text, mapping.external)
    expect(hits).toEqual([{ keyword: '국제처', line: 2 }])
  })
  it('외부 키워드 없으면 빈 배열', () => {
    expect(findExternalMentions('구글폼만', mapping.external)).toEqual([])
  })
})

describe('checkDoc', () => {
  const mapping: ScopeMapping = {
    teams: {
      '교환담당팀': { owns: ['구글폼', '카드뉴스'] },
      '예산팀': { owns: ['예산안'] },
    },
    external: ['국제처', '총학'],
  }

  it('팀 소속 문서에 외부 키워드 → 위반', () => {
    const violations = checkDoc(
      '교환담당팀/사전공유 (교환담당팀용).md',
      '### 회의 전 할 일\n국제처 컨택 사항 정리\n',
      mapping
    )
    expect(violations.length).toBe(1)
    expect(violations[0].type).toBe('external-in-team-doc')
    expect(violations[0].keyword).toBe('국제처')
    expect(violations[0].team).toBe('교환담당팀')
  })

  it('팀 소속 아니면 외부 키워드 있어도 위반 아님', () => {
    const violations = checkDoc(
      '안건서.md',
      '국제처 컨택은 임원진 경유\n',
      mapping
    )
    expect(violations).toEqual([])
  })

  it('팀 소속 문서에 외부 키워드 없으면 통과', () => {
    const violations = checkDoc(
      '교환담당팀/x.md',
      '구글폼 제작\n',
      mapping
    )
    expect(violations).toEqual([])
  })
})
```

- [ ] **Step 3: 테스트 실패 확인**

Run:
```bash
cd sportsday-hub && npx vitest run tests/scope-mapping.test.ts
```
Expected: FAIL — 모듈 없음 (import 에러).

- [ ] **Step 4: scope-mapping lib 구현**

`sportsday-hub/scripts/lib/scope-mapping.ts` 생성:

```typescript
/**
 * 소관 매핑 검증 — 순수 로직 모듈
 *
 * 마스터 지침의 YAML 매핑을 파싱하고, 회의/사전공유 문서에서
 * 외부 컨택 키워드가 특정 팀에 월권 할당되는지 검사한다.
 */
import yaml from 'js-yaml'

export type TeamName =
  | '기획관리팀'
  | '컨텐츠팀'
  | '예산팀'
  | '교환담당팀'
  | '타임라인/인원관리팀'

export interface ScopeMapping {
  teams: Record<string, { owns: string[] }>
  external: string[]
}

export interface Violation {
  type: 'external-in-team-doc'
  file: string
  line: number
  keyword: string
  team: string
  detail: string
}

// 팀명 별칭 — 파일명/폴더명 매칭용
const TEAM_ALIASES: Array<{ team: TeamName; aliases: string[] }> = [
  { team: '기획관리팀', aliases: ['기획관리팀', '기획관리'] },
  { team: '컨텐츠팀', aliases: ['컨텐츠팀', '컨텐츠'] },
  { team: '예산팀', aliases: ['예산팀'] },
  { team: '교환담당팀', aliases: ['교환담당팀', '교환'] },
  { team: '타임라인/인원관리팀', aliases: ['타임라인', '인원관리'] },
]

/** 마스터 마크다운에서 "소관 매핑" 제목 아래 첫 yaml 펜스 추출 */
export function extractMappingYaml(markdown: string): string {
  const headingIdx = markdown.indexOf('소관 매핑')
  if (headingIdx === -1) return ''
  const after = markdown.slice(headingIdx)
  const fenceMatch = after.match(/```yaml\n([\s\S]*?)```/)
  return fenceMatch ? fenceMatch[1] : ''
}

/** YAML 텍스트 → ScopeMapping */
export function parseMapping(yamlText: string): ScopeMapping {
  if (!yamlText.trim()) {
    throw new Error('매핑 YAML이 비어 있습니다. 마스터 지침의 소관 매핑 블록을 확인하세요.')
  }
  const raw = yaml.load(yamlText) as {
    teams: Record<string, { owns: string[] }>
    external_contacts?: { 대상?: string[] }
  }
  return {
    teams: raw.teams,
    external: raw.external_contacts?.대상 ?? [],
  }
}

/** 파일 경로에서 소속 팀 추론 (없으면 null) */
export function inferDocTeam(filePath: string): TeamName | null {
  const normalized = filePath.replace(/\\/g, '/')
  for (const { team, aliases } of TEAM_ALIASES) {
    if (aliases.some((a) => normalized.includes(a))) return team
  }
  return null
}

/** 본문에서 외부 키워드 등장 위치 탐지 */
export function findExternalMentions(
  text: string,
  keywords: string[]
): Array<{ keyword: string; line: number }> {
  const lines = text.split('\n')
  const hits: Array<{ keyword: string; line: number }> = []
  lines.forEach((line, i) => {
    for (const kw of keywords) {
      if (line.includes(kw)) {
        hits.push({ keyword: kw, line: i + 1 })
        break // 한 줄에 여러 키워드면 첫 것만
      }
    }
  })
  return hits
}

/**
 * 단일 문서 검사.
 * 규칙: 팀 소속 문서(파일명/경로 기반)에 외부 컨택 키워드가 나오면 위반.
 * (외부 컨택은 5팀 소관이 아니므로 팀 문서에 있으면 안 됨)
 */
export function checkDoc(
  filePath: string,
  markdown: string,
  mapping: ScopeMapping
): Violation[] {
  const team = inferDocTeam(filePath)
  if (!team) return [] // 팀 소속 문서가 아니면 검사 안 함
  const mentions = findExternalMentions(markdown, mapping.external)
  return mentions.map((m) => ({
    type: 'external-in-team-doc' as const,
    file: filePath,
    line: m.line,
    keyword: m.keyword,
    team,
    detail: `"${m.keyword}" 은(는) 외부 컨택(5팀 소관 아님)인데 ${team} 문서에 등장. 임원진/기관팀 경유 영역입니다.`,
  }))
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run:
```bash
cd sportsday-hub && npx vitest run tests/scope-mapping.test.ts
```
Expected: PASS (전체 테스트 통과).

- [ ] **Step 6: 커밋**

```bash
git add sportsday-hub/package.json sportsday-hub/package-lock.json \
        sportsday-hub/scripts/lib/scope-mapping.ts \
        sportsday-hub/tests/scope-mapping.test.ts
git commit -m "feat(scope): 소관 매핑 검증 순수 로직 + 단위 테스트

js-yaml devDep 추가. scope-mapping lib: 매핑 YAML 파싱, 팀 소속 추론,
외부 키워드 탐지, 팀 문서 내 외부 키워드 = 월권 위반 판정."
```

---

### Task 4: verify-scope-ownership CLI + package.json verify 스크립트

**Files:**
- Create: `sportsday-hub/scripts/verify-scope-ownership.ts`
- Modify: `sportsday-hub/package.json` (`verify` 스크립트)

**Interfaces:**
- Consumes: Task 3의 `extractMappingYaml`, `parseMapping`, `checkDoc`, `ScopeMapping`
- Produces: `npx tsx scripts/verify-scope-ownership.ts` 실행 시 exit 0(정상)/1(위반). `npm run verify`로 두 검증 통합 실행.

- [ ] **Step 1: CLI 스크립트 작성**

`sportsday-hub/scripts/verify-scope-ownership.ts` 생성:

```typescript
/**
 * 소관 정합성 검증 스크립트
 *
 * 마스터 지침의 소관 매핑을 읽어, 회의/사전공유/안건서 문서에서
 * 외부 컨택 키워드(국제처 등)가 특정 팀에 월권 할당되는지 검증한다.
 *
 * 실행: npx tsx scripts/verify-scope-ownership.ts
 */
import { readFileSync, readdirSync, statSync } from 'fs'
import { resolve, join, relative } from 'path'
import {
  extractMappingYaml,
  parseMapping,
  checkDoc,
  type Violation,
} from './lib/scope-mapping'

const HUB_ROOT = resolve(__dirname, '..')
const REPO_ROOT = resolve(HUB_ROOT, '..')
const DOC_ROOT = resolve(REPO_ROOT, '26-2 Sports Day')

// 검증 대상 문서 패턴 (회의/사전공유/안건서 관련 마크다운)
const TARGET_GLOBS = ['**/*.md']

function walkMd(dir: string): string[] {
  const out: string[] = []
  for (const entry of readdirSync(dir)) {
    if (entry.startsWith('.')) continue
    const full = join(dir, entry)
    const st = statSync(full)
    if (st.isDirectory()) {
      out.push(...walkMd(full))
    } else if (entry.endsWith('.md')) {
      out.push(full)
    }
  }
  return out
}

function main() {
  // 1. 마스터에서 매핑 로드
  const masterPath = resolve(DOC_ROOT, '00_기획지침_마스터.md')
  const masterMd = readFileSync(masterPath, 'utf-8')
  const yamlText = extractMappingYaml(masterMd)
  if (!yamlText) {
    console.error('❌ 마스터 지침에서 소관 매핑 블록을 찾을 수 없습니다.')
    process.exit(1)
  }
  const mapping = parseMapping(yamlText)

  console.log('소관 정합성 검증 시작...\n')
  console.log(`매핑 로드: 팀 ${Object.keys(mapping.teams).length}개 / 외부 키워드 ${mapping.external.length}개\n`)

  // 2. 대상 문서 스캔
  const docs = walkMd(DOC_ROOT)
  let totalViolations = 0

  for (const absPath of docs) {
    const relPath = relative(REPO_ROOT, absPath).replace(/\\/g, '/')
    const md = readFileSync(absPath, 'utf-8')
    const violations: Violation[] = checkDoc(relPath, md, mapping)
    if (violations.length > 0) {
      for (const v of violations) {
        console.log(`❌ 월권 의심: ${v.file}:${v.line}`)
        console.log(`   ${v.detail}`)
        totalViolations++
      }
    }
  }

  console.log('')
  if (totalViolations === 0) {
    console.log('✅ 소관 정합성 이상 없음 — 외부 컨택 키워드가 팀 문서에 월권 할당되지 않았습니다.')
    process.exit(0)
  } else {
    console.log(`${totalViolations}건의 월권 의심 발견. 해당 팀 소관인지 매핑과 대조하세요.`)
    process.exit(1)
  }
}

main()
```

- [ ] **Step 2: package.json에 verify 스크립트 추가**

`sportsday-hub/package.json`의 `scripts`에 추가:

```json
    "verify": "tsx scripts/verify-guideline-sync.ts && tsx scripts/verify-scope-ownership.ts"
```

(`migrate:md` 라인 다음에 추가)

- [ ] **Step 3: CLI 실행 확인 (현재 문서 대상 — Task 1/2 적용 후라 위반 0이어야 함)**

Run:
```bash
cd sportsday-hub && npx tsx scripts/verify-scope-ownership.ts
```
Expected: exit 0, `✅ 소관 정합성 이상 없음`. (Task 1에서 교환담당팀 문서의 국제처를 제거했으므로 위반 0.)

- [ ] **Step 4: 커밋**

```bash
git add sportsday-hub/scripts/verify-scope-ownership.ts sportsday-hub/package.json
git commit -m "feat(scope): verify-scope-ownership CLI + verify 통합 스크립트

회의/사전공유/안건서 마크다운을 스캔해 팀 문서 내 외부 컨택 키워드를
월권 의심으로 보고. npm run verify로 동기화+소관 검증 통합 실행."
```

---

### Task 5: 종료 검증 — 합성 위반 픽스처로 스크립트가 월권을 잡는지 확인

**Files:**
- Create: `sportsday-hub/tests/fixtures/scope/violation-sample.md` (합성 위반 — 커밋하지 않거나 .gitignore, 이 태스크에서만 사용)

**목적**: Task 1 회수 후엔 실제 문서에 위반이 없으므로, 스크립트가 진짜로 월권을 잡는지 별도 샘플로 증명. 이 태스크 후 샘플은 삭제.

- [ ] **Step 1: 합성 위반 샘플 생성 (임시)**

`sportsday-hub/tests/fixtures/scope/violation-sample.md` 생성:

```markdown
# 교환담당팀 가짜 사전공유 (검증 테스트용)

## 회의 전까지 할 것
### 국제처 컨택 사항 정리
- 천막 / First Aid / 대여물품
- 총학 물품도 여기서 정리
```

(파일명/내용에 교환담당팀 컨텍스트 + 국제처·총학 외부 키워드 — 월권 패턴)

- [ ] **Step 2: lib 직접 검사로 위반 잡힘 확인**

`sportsday-hub/tests/scope-mapping.test.ts` 끝에 임시 검증을 추가하는 대신, 빠른 직접 확인:

Run:
```bash
cd sportsday-hub && npx tsx -e "
import { readFileSync } from 'fs';
import { extractMappingYaml, parseMapping, checkDoc } from './scripts/lib/scope-mapping';
const master = readFileSync('../26-2 Sports Day/00_기획지침_마스터.md','utf-8');
const m = parseMapping(extractMappingYaml(master));
const sample = readFileSync('./tests/fixtures/scope/violation-sample.md','utf-8');
const v = checkDoc('교환담당팀/violation-sample.md', sample, m);
console.log('위반 수:', v.length);
v.forEach(x => console.log(' -', x.keyword, '@line', x.line, '→', x.team));
"
```
Expected: `위반 수: 2` (국제처, 총학 각각). 둘 다 team이 `교환담당팀`.

- [ ] **Step 3: 임시 샘플 삭제**

```bash
rm -rf sportsday-hub/tests/fixtures/scope
```

(필요하면 fixtures 폴더 자체 제거. 실제 문서가 스크립트의 검증 대상이므로 영구 픽스처 불필요.)

- [ ] **Step 4: 최종 통합 검증**

Run:
```bash
cd sportsday-hub && npm run verify
```
Expected: `verify-guideline-sync` ✅ + `verify-scope-ownership` ✅, exit 0.

- [ ] **Step 5: 전체 테스트 통과 확인**

Run:
```bash
cd sportsday-hub && npm test
```
Expected: 기존 테스트 + scope-mapping 테스트 전부 PASS.

- [ ] **Step 6: 최종 커밋 (있을 경우)**

임시 파일은 삭제했으므로 커밋 대상 없음. working tree clean 확인:
```bash
git status
```
Expected: nothing to commit, working tree clean.

---

## Self-Review

**1. Spec coverage:**
- 섹션 1 (매핑 구조) → Task 2 Step 1 ✅
- 섹션 2 (즉각 수정: 안건서·사전공유) → Task 1 Step 1-2 ✅
- 섹션 2 (안내문 수정 불필요) → 명시적 수정 태스크 없음 (의도적 — 스펙이 수정 불필요라고 함) ✅
- 섹션 2 (회의록 폴더) → Task 1 Step 3 ✅
- 섹션 3 (검증 스크립트) → Task 3 + Task 4 ✅
- 섹션 4 (전사문 워크플로우) → 회의록 폴더(Task 1 Step 3) + 매핑에 "전사문 기반 갱신" 주석(Task 2 Step 1)으로 인프라 구축. 런타임 워크플로우는 AI가 회의 시 수행하는 절차라 코드 태스크 아님 — 스펙에 절차로 문서화됨. ✅
- 섹션 5 (마스터 원칙) → Task 2 Step 1-2 ✅
- 파일 변경 요약표 7개 → Task 1(2) + Task 2(2) + Task 3(3) + Task 4(2) = 커버 ✅ (회의록 .gitkeep 포함)

**2. Placeholder scan:** TBD/TODO 없음. 모든 코드 스텝에 실제 코드 포함. ✅

**3. Type consistency:**
- `ScopeMapping` (Task 3 정의) → Task 4 import 사용, 필드 `teams`/`external` 일치 ✅
- `Violation.type = 'external-in-team-doc'` (Task 3) → Task 4 CLI가 같은 타입 처리 ✅
- `checkDoc(filePath, markdown, mapping)` 시그니처 (Task 3) → Task 4·테스트 동일 호출 ✅
- `extractMappingYaml`/`parseMapping` (Task 3) → Task 4 CLI 동일 사용 ✅

이슈 없음.
